CREATE TABLE orders (
                              id BIGINT AUTO_INCREMENT PRIMARY KEY,
                              customer VARCHAR(255) NULL,
                              product VARCHAR(255) NULL,
                              quantity INT NULL
);